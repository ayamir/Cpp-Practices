#include <bits/stdc++.h>
#include <climits>

using namespace std;

class Solution {
  public:
    int maxOperations(vector<int> &nums, int k) {
        int res = INT_MIN;

        return res == INT_MIN ? 0 : res;
    }
};

int main(int argc, char *argv[]) {
    Solution s;
    return 0;
}
